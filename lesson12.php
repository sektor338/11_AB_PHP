<?php
/*
 * superglobal variables
 * $_GET - url query string
 * $_POST - form data
 * $_SESSION - session data (stored on the server)
 * $_COOKIE - cookie data (stored on the client)
 * $_SERVER - server data
 * * $_SERVER['DOCUMENT_ROOT'] - root folder of the project
 * * $_SERVER['PHP_SELF'] - current file
 * * $_SERVER['SERVER_NAME'] - server name
 *  * $_SERVER['REQUEST_METHOD'] - request method
 * $_FILES - file data
 *
 *
 *
 *
 *
 *
 *
 *
 *
 * */